using System;

namespace {{toNameSpace info.title}} {


{{#each components.schemas}}
    {{#if description}}
    /**
    * {{description}}
    */
    {{/if}}
    public class {{capitaliseFirstLetter @key}} 
    {
      {{#each properties}}
        {{#if description}}
        // {{description}}
        {{/if}}
        {{#if example}}
        // e.g. {{example}}
        {{/if}}
        public {{safeTypeConvert this}} {{@key}} { get; set; }
{{#unless @last}}

{{/unless}}
      {{/each}}
    }
{{#unless @last}}

{{/unless}}
{{/each}}
}