using System;

namespace {{toSafeName info.title}} {


{{#each components.schemas}}
    {{#if description}}
    /**
    * {{description}}
    */
    {{/if}}
    public class {{toSafeName @key}} 
    {
      {{#each properties}}
        {{#if description}}
        // {{description}}
        {{/if}}
        {{#if example}}
        // e.g. {{example}}
        {{/if}}
        public {{safeTypeConvert this}} {{@key}} { get; set; }
{{#unless @last}}

{{/unless}}
      {{/each}}
    }
{{#unless @last}}

{{/unless}}
{{/each}}
}