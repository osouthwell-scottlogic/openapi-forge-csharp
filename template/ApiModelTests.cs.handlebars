using System;
using Xunit;
using AutoFixture;
using {{toNameSpace info.title}};

namespace {{toNameSpace info.title}}Tests {


{{#each components.schemas}}
    public class {{@key}}Tests
    {
        [Fact]
        public void CanBeInstantiated()
        {
            var sut = new {{@key}}();
            Assert.NotNull(sut);
        }

        [Fact]
        public void CanSetProperties()
        {
            var fixture = new Fixture();
            var sut = fixture.Create<{{@key}}>();
            Assert.NotNull(sut);
        }
    }
{{#unless @last}}

{{/unless}}
{{/each}}
}